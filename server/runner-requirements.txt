# The figure runner's python environment — everything partner scripts may
# import, pinned by major version. Extending this list is a deploy-time
# decision (see README → Live figures).
pandas>=2.2,<3
numpy>=1.26,<3
matplotlib>=3.8,<4
requests>=2.31,<3
